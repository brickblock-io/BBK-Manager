// @flow
import Toolbar from '@material-ui/core/Toolbar'
import { withStyles } from '@material-ui/core'
import styles from './styles'

export const StyledToolbar = withStyles(styles)(Toolbar)

export default StyledToolbar
